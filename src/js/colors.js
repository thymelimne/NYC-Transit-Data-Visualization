//==========================================
// Colors
const periwinkle = "#CCCCFF"
const mauve = "#E0B0FF"

//var colors = chroma.scale(["blue",periwinkle,"crimson"]).mode('lrgb')
var colors = chroma.scale(["violet","blue","lightgreen","yellow","orange","crimson"]).mode('lrgb')

/*
    TO USE:
    >> color = colors(n).hex()

    ^ Yields a string, which is a color hex value.
    n is a number from 0 to 1.
//*/